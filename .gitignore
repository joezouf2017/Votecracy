# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/

# Node
node_modules/
frontend/dist/

# Claude Code local config and project context (local only)
CLAUDE.md
.claude/

# OS
.DS_Store
Thumbs.db

# Bulk source corpora, cached locally. Voteview alone is 29 MB, it is
# reproducible from one URL, and nothing in the repo should carry it.
backend/.cache/

# Secrets. settings.py reads .env, so without this the first person to create
# one commits their API keys. .env.example carries the shape, never the values.
.env
.env.*
!.env.example
