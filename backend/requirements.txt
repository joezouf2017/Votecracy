fastapi==0.115.0
uvicorn[standard]==0.30.6

# Phase 2 — distributed voting.
# redis: atomic dedupe + vote tally on the hot path (see cache.py).
# sqlalchemy + psycopg: durable vote log written off the request path (see db.py).
redis==5.0.8
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
# alembic: create_all() only ever creates missing tables, so it cannot add a
# column to a table that already exists. Phase 3 changes existing tables.
alembic==1.13.3

# Phase 3 — content pipeline.
# pgvector: the SQLAlchemy `vector` column type. Embeddings live in the same
# Postgres instance as everything else rather than in a separate vector DB —
# one thing to run, back up and migrate.
pgvector==0.3.6
